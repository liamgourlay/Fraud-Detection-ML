# Fraud-Detection-ML
Developed a machine learning pipeline to detect fraudulent credit card transactions using the Kaggle European cardholder dataset. The project compares multiple supervised learning algorithms, evaluates them using ROC-AUC, precision and recall, and applies hyperparameter tuning to optimise model performance.
